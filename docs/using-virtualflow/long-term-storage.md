# The Completed Workflow

## Long-Term Storage

For long-term storage, one can create a compressed tar archive of the VirtualFlow root folder:

```
tar -xvzf VF.tar.gz <VirtualFlow root folder>
```

Alternatively, one might to choose only to store the output ligand databases, which are located in the `output-files/complete`folder.
